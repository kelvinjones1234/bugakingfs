"use client";

import React from "react";
import { Main } from "./components/Main"; 

const DashboardPage = () => {
  // logic for the dashboard widgets can go here if needed,
  // or inside the <Main /> component itself.
  
  return <Main />;
};

export default DashboardPage;